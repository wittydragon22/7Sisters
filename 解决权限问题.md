# 解决 GitHub 推送权限问题

## 🔍 问题分析

推送时遇到 `Permission denied` 错误，可能的原因：

1. **Token 权限不足** - Token 需要 `repo` 权限
2. **Token 已过期或被撤销**
3. **Token 格式问题**

## ✅ 解决方案

### 方案一：检查并更新 Token 权限

1. **访问 Token 设置**
   - 访问：https://github.com/settings/tokens
   - 找到你的 token（如果找不到，说明可能已被删除）

2. **检查权限**
   - 确保 token 有 **`repo`** 权限
   - 如果没有，需要创建新的 token

3. **创建新 Token（如果需要）**
   - 点击 "Generate new token (classic)"
   - 勾选 **`repo`** 权限（这会自动勾选所有 repo 相关权限）
   - 点击 "Generate token"
   - **复制新 token**（只显示一次！）

### 方案二：使用 Git Credential Manager

如果安装了 Git for Windows，可以使用凭证管理器：

```bash
# 清除已保存的凭证
git credential-manager-core erase
# 或者
git credential reject https://github.com

# 然后推送，会提示输入用户名和 token
git push -u origin main
# 用户名：wittydragon22
# 密码：输入你的 Personal Access Token
```

### 方案三：使用 SSH（推荐长期使用）

1. **生成 SSH 密钥**（如果还没有）
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. **添加 SSH 密钥到 GitHub**
   - 复制公钥：`cat ~/.ssh/id_ed25519.pub`
   - 访问：https://github.com/settings/keys
   - 点击 "New SSH key"
   - 粘贴公钥并保存

3. **更改远程仓库为 SSH**
   ```bash
   git remote set-url origin git@github.com:wittydragon22/7Sisters.git
   git push -u origin main
   ```

## 🚀 快速解决（推荐）

**最简单的方法**：

1. **创建新的 Token**
   - 访问：https://github.com/settings/tokens
   - Generate new token (classic)
   - 勾选 `repo` 权限
   - 生成并复制新 token

2. **使用新 token 推送**
   ```bash
   git remote set-url origin https://新token@github.com/wittydragon22/7Sisters.git
   git push -u origin main
   ```

## 📝 当前仓库信息

- **仓库地址**：https://github.com/wittydragon22/7Sisters
- **远程名称**：origin
- **分支**：main

## ⚠️ 重要提示

你的 Personal Access Token 已经暴露，为了安全：
1. **立即撤销旧 token**
2. **创建新 token**
3. **不要分享新 token**

