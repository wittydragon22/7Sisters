# 快速推送指南（使用你的 Token）

## 🚀 方法一：使用 PowerShell 脚本（推荐）

1. **在 GitHub 创建仓库**
   - 访问 https://github.com/new
   - 输入仓库名（例如：`pleiades-website`）
   - 选择 Public 或 Private
   - **不要**勾选 "Initialize with README"
   - 点击 "Create repository"

2. **运行脚本**
   - 右键点击 `push-with-token.ps1`
   - 选择 "使用 PowerShell 运行"
   - 或者打开 PowerShell，运行：
     ```powershell
     .\push-with-token.ps1
     ```

3. **按提示输入**
   - GitHub 用户名
   - 仓库名称

4. **完成！**
   - 脚本会自动完成所有步骤

---

## 📝 方法二：手动命令

如果你更喜欢手动操作，按以下步骤：

### 1. 初始化 Git（如果还没初始化）

```bash
git init
git add .
git commit -m "Initial commit: 昂宿星团网站"
```

### 2. 在 GitHub 创建仓库

访问 https://github.com/new 创建新仓库，记住你的：
- 用户名（例如：`your-username`）
- 仓库名（例如：`pleiades-website`）

### 3. 连接并推送

```bash
# 替换为你的实际信息
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main

# 使用 token 推送（替换为你的信息）
git push -u origin main
```

当提示输入用户名时，输入你的 GitHub 用户名。
当提示输入密码时，**输入你的 Personal Access Token**（不是密码）。

---

## 🔐 使用 Token 的另一种方法（URL 方式）

你也可以直接在 URL 中包含 token：

```bash
# 格式：https://token@github.com/用户名/仓库名.git
git remote add origin https://YOUR_TOKEN@github.com/你的用户名/你的仓库名.git

git branch -M main
git push -u origin main
```

---

## ⚠️ 重要安全提示

**你的 Personal Access Token 已经暴露了！**

为了安全，建议：

1. **立即撤销当前 token**
   - 访问：https://github.com/settings/tokens
   - 找到这个 token 并删除

2. **创建新的 token**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - 勾选 `repo` 权限
   - 生成新 token

3. **保护新 token**
   - 不要分享给任何人
   - 不要提交到代码仓库
   - 使用环境变量或 Git Credential Manager 存储

---

## ✅ 推送成功后

访问你的仓库：`https://github.com/你的用户名/你的仓库名`

然后就可以：
- 使用 Vercel/Netlify 部署网站
- 分享链接给"夏夏"

---

## 🆘 遇到问题？

### 问题：提示 "repository not found"
- 确保仓库已创建
- 检查用户名和仓库名是否正确
- 确保 token 有 `repo` 权限

### 问题：提示 "authentication failed"
- 检查 token 是否正确
- 确保 token 未过期
- 尝试重新生成 token

### 问题：提示 "remote origin already exists"
```bash
git remote remove origin
# 然后重新添加
```

### 问题：推送被拒绝
如果远程仓库有内容，先拉取：
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

