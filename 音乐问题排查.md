# 音乐播放问题排查

## 🔍 检查步骤

### 1. 清除浏览器缓存并硬刷新

- **Chrome/Edge**: 按 `Ctrl + Shift + R` 或 `Ctrl + F5`
- **Firefox**: 按 `Ctrl + Shift + R`
- 或者：
  - 按 `F12` 打开开发者工具
  - 右键点击刷新按钮
  - 选择"清空缓存并硬性重新加载"

### 2. 重启开发服务器

```bash
# 停止当前服务器（Ctrl + C）
# 然后重新启动
npm run dev
```

### 3. 检查浏览器控制台

1. 按 `F12` 打开开发者工具
2. 切换到 "Console" 标签
3. 查看是否有错误信息
4. 应该能看到：
   - "音频加载成功: http://localhost:xxxx/background-music.mp3" （如果成功）
   - 或 "音频加载失败" （如果失败）

### 4. 检查网络请求

1. 在开发者工具中切换到 "Network" 标签
2. 刷新页面
3. 搜索 "background-music.mp3"
4. 查看：
   - 文件是否成功加载（状态码应该是 200）
   - 如果失败，查看错误信息

### 5. 直接访问音乐文件

在浏览器地址栏输入：
```
http://localhost:5173/background-music.mp3
```
（端口号可能不同，根据你的开发服务器端口）

如果能直接播放，说明文件路径正确。

## 🛠️ 可能的解决方案

### 如果文件加载失败

1. **检查文件是否存在**
   - 确认 `public/background-music.mp3` 文件存在
   - 文件大小应该 > 0

2. **检查文件格式**
   - 确保是有效的 MP3 文件
   - 可以尝试用其他播放器打开确认

3. **尝试重新添加文件**
   - 删除 `public/background-music.mp3`
   - 重新复制你的音乐文件到 `public` 文件夹
   - 确保文件名完全一致（包括大小写）

### 如果还是播放错误的音乐

可能是浏览器缓存了旧的音乐文件。尝试：
1. 清除浏览器所有缓存
2. 使用隐私模式/无痕模式打开网站
3. 或者使用不同的浏览器测试

## 📝 调试信息

我已经在代码中添加了调试信息，打开浏览器控制台（F12）应该能看到：
- 音频加载成功/失败的消息
- 尝试加载的文件路径

这些信息可以帮助定位问题。

